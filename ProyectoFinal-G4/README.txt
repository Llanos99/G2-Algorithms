Antes de correr el programa, se recomienda revisar las librerias usadas e instalarlas desde terminal si es necesario. 
Por ejemplo, para la libreria mpl_toolkits.basemap, esta puede ser instalada desde terminal usando el comando:

pip install basemap
